<%= form_with(model: user_chat) do |form| %>
  <% if user_chat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_chat.errors.count, "error") %> prohibited this user_chat from being saved:</h2>

      <ul>
        <% user_chat.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :chat_id %>
    <%= form.number_field :chat_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
